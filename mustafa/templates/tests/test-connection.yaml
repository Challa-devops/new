apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mustafa.fullname" . }}-test-connection"
  labels:
    {{- include "mustafa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mustafa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
