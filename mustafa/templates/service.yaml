apiVersion: v1
kind: Service
metadata:
  name: {{ include "mustafa.fullname" . }}
  labels:
    {{- include "mustafa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mustafa.selectorLabels" . | nindent 4 }}
